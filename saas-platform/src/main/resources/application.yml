server:
  port: 8080

spring:
  application:
    name: saas-platform

  datasource:
    url: jdbc:postgresql://localhost:5432/saas_platform
    username: postgres
    password: kamal@PSQL1
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true